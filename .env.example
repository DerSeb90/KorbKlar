# KorbKlar — Beispielkonfiguration. Kopieren nach .env.
#
# Der erste Block gilt immer. Die weiteren nur, wenn du die passende
# Compose-Datei dazunimmst:
#
#   docker compose up -d                                        nur KorbKlar
#   docker compose -f compose.yml -f compose.kitchenowl.yml …    + Einkaufsliste
#   docker compose -f compose.yml -f compose.proxy.yml …         + HTTPS
#
# Siehe docs/deployment.de.md.

# Welche Compose-Dateien dazugehören. Einmal hier eingetragen, genügt wieder
# ein blankes "docker compose up -d" — auch für pull, logs, down.
# Nur KorbKlar: Zeile weglassen.
# Getrennt wird mit ":" unter Linux und macOS, mit ";" unter Windows.
#COMPOSE_FILE=compose.yml:compose.kitchenowl.yml:compose.proxy.yml


# ── KorbKlar (compose.yml) ───────────────────────────────────────────────────

# Host-Port des Docker-Dienstes. Intern bleibt der Container auf Port 8000.
SUPERMARKT_PORT=8000

# Optionale instanzweite Vorauswahl der Startseite. Ohne diese Werte bleibt
# der Auswahl-Prompt unverändert; persönliche Änderungen speichert der Browser.
SUPERMARKT_DEFAULT_POSTAL_CODE=
# Kommagetrennte Händlernamen, z. B. REWE,Lidl,Kaufland,dm
SUPERMARKT_DEFAULT_RETAILERS=

# Optionaler Schutz nur für POST /api/v1/compare.
SUPERMARKT_API_KEY=

# Laufzeitdaten. Für Docker passen die Standardwerte zum Volume /data.
SUPERMARKT_DATA_DIR=/data
SUPERMARKT_CACHE_DB=/data/supermarkt-cache.sqlite3
SUPERMARKT_SIGNING_SECRET_FILE=/data/.signing-secret
SUPERMARKT_ACCESS_TOKENS_FILE=/data/access-tokens.json
# Leer lassen, damit beim ersten Start automatisch ein persistenter Schlüssel erzeugt wird.
SUPERMARKT_SIGNING_SECRET=
SUPERMARKT_IMAGE_CACHE_DIR=/data/supermarkt-images
SUPERMARKT_KAUFLAND_CACHE_DIR=/data/kaufland
SUPERMARKT_REWE_CACHE_DIR=/data/rewe

# Angebots-Snapshot-Cache.
SUPERMARKT_CACHE_TTL_MINUTES=30
SUPERMARKT_CACHE_MAX_SNAPSHOTS=100
SUPERMARKT_RESULT_RETENTION_HOURS=168

# Netzwerk und Parallelität.
SUPERMARKT_TIMEOUT_SECONDS=25
SUPERMARKT_MARKTGURU_PAGE_SIZE=500
SUPERMARKT_MAX_WORKERS=8
# Leer verwendet automatisch korb-klar/<Version>.
SUPERMARKT_USER_AGENT=

# Browser für die Seiten, die JavaScript brauchen (ALDI Süd, Kaufland, REWE).
# Leer sucht selbst nach Chromium, Chrome oder Edge. Nur setzen, wenn der
# Browser an einem ungewöhnlichen Ort liegt.
SUPERMARKT_CHROMIUM=

# Filialzuordnungen. Die Angebotsdaten selbst bleiben frisch.
SUPERMARKT_KAUFLAND_STORE_CACHE_TTL_SECONDS=86400
SUPERMARKT_REWE_STORE_CACHE_TTL_SECONDS=86400

# Bildcache.
SUPERMARKT_IMAGE_CACHE_TTL_SECONDS=604800
SUPERMARKT_IMAGE_CACHE_MAX_BYTES=536870912
SUPERMARKT_IMAGE_MAX_FILE_BYTES=4194304


# ── Einkaufsliste (compose.kitchenowl.yml) ──────────────────────────────────

# Adresse der KitchenOwl-Instanz. Im mitgelieferten Stack geht KorbKlar über
# den Web-Container: das Backend spricht uwsgi, kein HTTP. Zeigst du auf eine
# KitchenOwl, die du schon betreibst, trägst du hier deren URL ein und
# brauchst compose.kitchenowl.yml gar nicht. Leer heißt: Anbindung aus.
SUPERMARKT_KITCHENOWL_URL=http://kitchenowl-web
# Long-lived Token aus KitchenOwl: Profil, Sitzungen, Long-lived Tokens.
# Leer heißt: Anbindung aus, die Oberfläche zeigt nichts davon.
SUPERMARKT_KITCHENOWL_TOKEN=
# Vorausgewählte Listen-ID. Leer lassen, um die Liste in der Oberfläche zu wählen.
SUPERMARKT_KITCHENOWL_LIST_ID=
# Nur auf 0 setzen, wenn KitchenOwl ein selbstsigniertes Zertifikat nutzt.
SUPERMARKT_KITCHENOWL_VERIFY_TLS=1
SUPERMARKT_KITCHENOWL_TIMEOUT_SECONDS=15
SUPERMARKT_KITCHENOWL_MAX_ITEMS=50
# Angebote auf einen bereits vorhandenen KitchenOwl-Artikel legen, statt für
# jeden Angebotsnamen einen neuen anzulegen. Der volle Name bleibt in der Notiz.
SUPERMARKT_KITCHENOWL_MATCH_ITEMS=1
# Händler als Kategorie führen, damit die Liste nach Markt gruppiert. Achtung:
# die Kategorie hängt am Artikel, nicht am Listeneintrag — ein Artikel wandert
# also mit, sobald ein anderer Markt ihn günstiger anbietet.
SUPERMARKT_KITCHENOWL_RETAILER_CATEGORIES=1
# KitchenOwl-Kategorien haben nur einen Namen und kein Bildfeld; ein Icon geht
# deshalb nur als Emoji davor. Leer lässt den Namen unverändert.
SUPERMARKT_KITCHENOWL_CATEGORY_PREFIX=🛒

# Pflicht, mindestens 32 Zeichen: openssl rand -base64 48
# KitchenOwl selbst prüft das nicht und liefe sonst mit leerem Schlüssel.
KITCHENOWL_JWT_SECRET=
# Öffentliche Adresse der KitchenOwl-Oberfläche, für korrekte Links.
KITCHENOWL_PUBLIC_URL=
# Host-Port der KitchenOwl-Oberfläche. Standard ist nur lokal erreichbar,
# weil normalerweise der Reverse Proxy davor steht. Ohne Proxy und von einem
# anderen Rechner aus: bewusst auf 0.0.0.0 setzen.
KITCHENOWL_PORT=8080
KITCHENOWL_BIND_ADDRESS=127.0.0.1
# Onboarding schließt sich selbst, sobald das erste Konto existiert. Auf true
# setzen, nachdem du deins angelegt hast: dann bleibt es auch bei geleertem
# Datenvolume zu. Die Registrierung ist ohnehin aus (OPEN_REGISTRATION=false).
KITCHENOWL_DISABLE_ONBOARDING=false


# ── Reverse Proxy (compose.proxy.yml) ───────────────────────────────────────

# Caddy holt und erneuert die Let's-Encrypt-Zertifikate selbst. Beide Werte
# sind Pflicht, sobald compose.proxy.yml dabei ist.
KORBKLAR_DOMAIN=
KORBKLAR_ACME_EMAIL=
# Schnittstelle, auf der KorbKlars eigener Port 8000 mit dem Proxy davor noch
# veröffentlicht wird. 127.0.0.1 reicht für curl auf dem Host; eine
# VPN-Adresse hält den Klartext-Weg für dieses Netz offen. Ohne
# compose.proxy.yml wird der Port unverändert auf allen Schnittstellen
# veröffentlicht und dieser Wert nicht gelesen.
KORBKLAR_BIND_ADDRESS=127.0.0.1
# Passwortschutz der Oberfläche über Caddy. Nur gelesen, wenn der
# basic_auth-Block in deploy/caddy/sites/korbklar.caddy eingeschaltet ist.
# Hash erzeugen: docker compose exec caddy caddy hash-password
KORBKLAR_BASIC_AUTH_HASH=
# Auf ./deploy/caddy/Caddyfile.kitchenowl setzen, um eine KitchenOwl im selben
# Compose-Projekt auf einer zweiten Domain mitzubedienen. Dann ist
# KORBKLAR_KITCHENOWL_DOMAIN Pflicht.
KORBKLAR_CADDYFILE=./deploy/caddy/Caddyfile
KORBKLAR_KITCHENOWL_DOMAIN=
